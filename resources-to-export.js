const add = (x, y) => x + y;
const multiplicate = (x, y) => x * y;

const personalInfo = {
  name: 'Claudiney Junior',
  github: 'claudineyjr',
  birthday: '1988-10-24'
};

module.exports = {
  add,
  multiplicate,
  personalInfo,
}